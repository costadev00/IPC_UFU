#include <stdio.h>
#include <stdlib.h>

int main()
{
    float valor1, valor2, media;
    scanf("%f", &valor1);
    scanf("%f", &valor2);
    media = (valor1 + valor2) / 2;
    printf("%f \n",media);
    return 0;
}
